/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.shopclues.com",
      "hips.hearstapps.com",
      "images.immediate.co.uk",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
